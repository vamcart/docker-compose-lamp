apiVersion: 1

datasources:
  - name: prometheus
    type: prometheus
    uid: prometheusdatasource
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "1s"
